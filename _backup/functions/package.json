{
  "name": "toolpool-functions",
  "description": "Cloud Functions for ToolPool",
  "scripts": {
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.3.0",
    "stripe": "^12.12.0"
  },
  "engines": {
    "node": "18"
  }
}
